version: '3.8'

services:
  postgres:
    build: .
    container_name: postgres-data-ready
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: mydb
